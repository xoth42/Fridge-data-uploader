# ---- Data source (READ-ONLY — script will never write here) ----
FRIGE_LOGS_DIR=C:\FridgeLogs

# ---- Prometheus Pushgateway on your EC2 manager server ----
PUSHGATEWAY_URL=ec2-3-16-30-195.us-east-2.compute.amazonaws.com:9091

# ---- Identity of THIS machine (unique per data source) ----
MACHINE_NAME=fridge-alpha

# ---- Optional: override the Prometheus job label (default: sensor_data) ----
# PUSH_JOB_NAME=sensor_data
