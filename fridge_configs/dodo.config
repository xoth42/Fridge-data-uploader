# Dodo fridge configuration (Oxford Instruments)
# INCOMPLETE — populated from diagnose.py report once available.
# Delete .diagnose_done in the script directory to re-run the diagnostic.

fridge:
  name: Dodo
  instance: fridge-dodo
  type: oxford_instruments   # data format differs from Bluefors — TBD

# TODO: fill in after reviewing diagnose.py dpaste output.
# Query Prometheus for the report URL:
#   fridge_diagnostic_url{instance="fridge-dodo"}

temperature_channels: {}
resistance_channels: {}

collect:
  status:     true
  flowmeter:  false   # unknown — update after diagnostic
  heaters:    false   # unknown — update after diagnostic
  channels:   false   # unknown — update after diagnostic
  maxigauge:  false   # unknown — update after diagnostic
